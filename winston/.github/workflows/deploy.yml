name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master  # 或者 main，取决于你的默认分支是哪个
    # 只有当代码变动在这些目录下时触发 (可选，也可以去掉这几行)
    # paths:
    #   - 'docs/**'
    #   - 'src/**'
    #   - 'docusaurus.config.js'

permissions:
  contents: write # 允许 Action 写入 gh-pages 分支

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./winston
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: winston/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build website
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # 发布的目录，Docusaurus 默认构建在 winston/build 文件夹
          publish_dir: ./winston/build
          # 这里的 user_name 和 user_email 可以随意写，用于 git commit 记录
          user_name: github-actions[bot]
          user_email: github-actions[bot]@users.noreply.github.com

